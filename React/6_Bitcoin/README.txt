
useState → stores data that can change.

useEffect → performs side effects (fetching data, logging, DOM manipulation, etc.).

They are often used together, but not required to be.

I needs Export default function for making the component available to other files.
or I can export the function at the bottom, useful to call the file the same name as the function